UAR_pan_arab_integration = {

	UAR_integrate_claim = {

		name = UAR_begin_integration

		icon = GFX_decision_generic_nationalism

		allowed = {
			original_tag = UAR
		}

		available = {
			FROM = {
				is_controlled_by = UAR
				OR = {
					is_island_state = yes
					any_neighbor_state = {
						is_controlled_by = ROOT
					}
				}
			}
		}

		state_target = yes

		on_map_mode = map_only

		#targets = {  }

		target_array = controlled_states

		target_trigger = {
			has_completed_focus = UAR_unification_through_war
			FROM = {
				is_claimed_by = ROOT
			}
		}

		cost = 35

		fire_only_once = yes

		days_remove = 50

		modifier = {
			political_power_gain = -0.25
		}

		visible = {
			tag = UAR
			has_completed_focus = UAR_unification_through_war
			always = yes
		}

		complete_effect = {

		}

		remove_effect = {
			FROM = {
				add_core_of = UAR
			}
		}
	}
}