italian_conquests = {
	take_ethiopia = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = ITA
		}
		available = {
			NOT = {
				has_war_with = ETH
			}
		}
		activation = {
			has_country_flag = italian_conquest_flag
			has_war_with = ETH
			is_ai = yes
		}
		timeout_effect = {
		}
	}
	ethiopia_survive = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = ETH
		}
		available = {
			ITA = { casualties_k > 85 }
			NOT = {
				any_core_state = { is_controlled_by = ITA }
			}
		}
		activation = {
			has_war_with = ITA
			ITA = { has_country_flag = italian_conquest_flag }
			is_ai = no
		}
		complete_effect = { white_peace = ITA add_war_support = 0.025 }
		timeout_effect = {
				white_peace = ITA
		}
	}

	take_somalia = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = ITA
		}
		available = {
			NOT = {
				has_war_with = SOM
			}
		}
		activation = {
			has_country_flag = italian_conquest_flag
			has_war_with = SOM
			is_ai = yes
		}
		timeout_effect = {
		}
	}
	somalia_survive = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = SOM
		}
		available = {
			ITA = { casualties_k > 85 }
			NOT = {
				any_core_state = { is_controlled_by = ITA }
			}
		}
		activation = {
			has_war_with = ITA
			ITA = { has_country_flag = italian_conquest_flag }
			is_ai = no
		}
		complete_effect = { white_peace = ITA add_war_support = 0.025 }
		timeout_effect = {
				white_peace = ITA
		}
	}

	take_lba = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = ITA
		}
		available = {
			NOT = {
				has_war_with = LBA
			}
		}
		activation = {
			has_country_flag = italian_conquest_flag
			has_war_with = LBA
			is_ai = yes
		}
		timeout_effect = {
		}
	}
	lba_survive = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = LBA
		}
		available = {
			ITA = { casualties_k > 85 }
			NOT = {
				any_core_state = { is_controlled_by = ITA }
			}
		}
		activation = {
			has_war_with = ITA
			ITA = { has_country_flag = italian_conquest_flag }
			is_ai = no
		}
		complete_effect = { white_peace = ITA add_war_support = 0.025 }
		timeout_effect = {
				white_peace = ITA
		}
	}
	take_gns = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = ITA
		}
		available = {
			NOT = {
				has_war_with = GNS
			}
		}
		activation = {
			has_country_flag = italian_conquest_flag
			has_war_with = GNS
			is_ai = yes
		}
		timeout_effect = {
		}
	}
	gns_survive = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = GNS
		}
		available = {
			ITA = { casualties_k > 85 }
			NOT = {
				any_core_state = { is_controlled_by = ITA }
			}
		}
		activation = {
			has_war_with = ITA
			ITA = { has_country_flag = italian_conquest_flag }
			is_ai = no
		}
		complete_effect = { white_peace = ITA add_war_support = 0.025 }
		timeout_effect = {
				white_peace = ITA
		}
	}
	take_isl = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = ITA
		}
		available = {
			NOT = {
				has_war_with = ISL
			}
		}
		activation = {
			has_country_flag = italian_conquest_flag
			has_war_with = ISL
			is_ai = yes
		}
		timeout_effect = {
		}
	}
	isl_survive = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = ISL
		}
		available = {
			ITA = { casualties_k > 85 }
			NOT = {
				any_core_state = { is_controlled_by = ITA }
			}
		}
		activation = {
			has_war_with = ITA
			ITA = { has_country_flag = italian_conquest_flag }
			is_ai = no
		}
		complete_effect = { white_peace = ITA add_war_support = 0.025 }
		timeout_effect = {
				white_peace = ITA
		}
	}
	take_fal = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = ITA
		}
		available = {
			NOT = {
				has_war_with = FAL
			}
		}
		activation = {
			has_country_flag = italian_conquest_flag
			has_war_with = FAL
			is_ai = yes
		}
		timeout_effect = {
		}
	}
	fal_survive = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = FAL
		}
		available = {
			ITA = { casualties_k > 85 }
			NOT = {
				any_core_state = { is_controlled_by = ITA }
			}
		}
		activation = {
			has_war_with = ITA
			ITA = { has_country_flag = italian_conquest_flag }
			is_ai = no
		}
		complete_effect = { white_peace = ITA add_war_support = 0.025 }
		timeout_effect = {
				white_peace = ITA
		}
	}
	take_ral = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = ITA
		}
		available = {
			NOT = {
				has_war_with = RAL
			}
		}
		activation = {
			has_country_flag = italian_conquest_flag
			has_war_with = RAL
			is_ai = yes
		}
		timeout_effect = {
		}
	}
	ral_survive = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = RAL
		}
		available = {
			ITA = { casualties_k > 85 }
			NOT = {
				any_core_state = { is_controlled_by = ITA }
			}
		}
		activation = {
			has_war_with = ITA
			ITA = { has_country_flag = italian_conquest_flag }
			is_ai = no
		}
		complete_effect = { white_peace = ITA add_war_support = 0.025 }
		timeout_effect = {
				white_peace = ITA
		}
	}
	take_acs = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = ITA
		}
		available = {
			NOT = {
				has_war_with = ACS
			}
		}
		activation = {
			has_country_flag = italian_conquest_flag
			has_war_with = ACS
			is_ai = yes
		}
		timeout_effect = {
		}
	}
	acs_survive = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = ACS
		}
		available = {
			ITA = { casualties_k > 85 }
			NOT = {
				any_core_state = { is_controlled_by = ITA }
			}
		}
		activation = {
			has_war_with = ITA
			ITA = { has_country_flag = italian_conquest_flag }
			is_ai = no
		}
		complete_effect = { white_peace = ITA add_war_support = 0.025 }
		timeout_effect = {
				white_peace = ITA
		}
	}

	take_tunisia = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = ITA
		}
		available = {
			NOT = {
				has_war_with = TUN
			}
		}
		activation = {
			has_country_flag = italian_conquest_flag
			has_war_with = TUN
			is_ai = yes
		}
		timeout_effect = {
		}
	}
	tunisia_survive = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = TUN
		}
		available = {
			ITA = { casualties_k > 85 }
			NOT = {
				any_core_state = { is_controlled_by = ITA }
			}
		}
		activation = {
			has_war_with = ITA
			ITA = { has_country_flag = italian_conquest_flag }
			is_ai = no
		}
		complete_effect = { white_peace = ITA add_war_support = 0.025 }
		timeout_effect = {
				white_peace = ITA
		}
	}

	take_albania = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = ITA
		}
		available = {
			NOT = {
				has_war_with = ALB
			}
		}
		activation = {
			has_country_flag = italian_conquest_flag
			has_war_with = ALB
			is_ai = yes
		}
		timeout_effect = {
		}
	}
	albania_survive = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = ALB
		}
		available = {
			ITA = { casualties_k > 85 }
			NOT = {
				any_core_state = { is_controlled_by = ITA }
			}
		}
		activation = {
			has_war_with = ITA
			ITA = { has_country_flag = italian_conquest_flag }
			is_ai = no
		}
		complete_effect = { white_peace = ITA add_war_support = 0.025 }
		timeout_effect = {
				white_peace = ITA
		}
	}

	take_uar = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = ITA
		}
		available = {
			NOT = {
				has_war_with = UAR
			}
		}
		activation = {
			has_country_flag = italian_conquest_flag
			has_war_with = UAR
			is_ai = yes
		}
		timeout_effect = {
		}
	}
	uar_survive = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = UAR
		}
		available = {
			ITA = { casualties_k > 85 }
			NOT = {
				any_core_state = { is_controlled_by = ITA }
			}
		}
		activation = {
			has_war_with = ITA
			ITA = { has_country_flag = italian_conquest_flag }
			is_ai = no
		}
		complete_effect = { white_peace = ITA add_war_support = 0.025 }
		timeout_effect = {
				white_peace = ITA
		}
	}

	take_egypt = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = ITA
		}
		available = {
			NOT = {
				has_war_with = EGY
			}
		}
		activation = {
			has_country_flag = italian_conquest_flag
			has_war_with = EGY
			is_ai = yes
		}
		timeout_effect = {
		}
	}
	egypt_survive = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = EGY
		}
		available = {
			ITA = { casualties_k > 85 }
			NOT = {
				any_core_state = { is_controlled_by = ITA }
			}
		}
		activation = {
			has_war_with = ITA
			ITA = { has_country_flag = italian_conquest_flag }
			is_ai = no
		}
		complete_effect = { white_peace = ITA add_war_support = 0.025 }
		timeout_effect = {
				white_peace = ITA
		}
	}

	take_syria_ita = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = ITA
		}
		available = {
			NOT = {
				has_war_with = SYR
			}
		}
		activation = {
			has_country_flag = italian_conquest_flag
			has_war_with = SYR
			is_ai = yes
		}
		timeout_effect = {
		}
	}
	syria_survive_ita = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = SYR
		}
		available = {
			ITA = { casualties_k > 85 }
			NOT = {
				any_core_state = { is_controlled_by = ITA }
			}
		}
		activation = {
			has_war_with = ITA
			ITA = { has_country_flag = italian_conquest_flag }
			is_ai = no
		}
		complete_effect = { white_peace = ITA add_war_support = 0.025 }
		timeout_effect = {
				white_peace = ITA
		}
	}

	take_greece = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = ITA
		}
		available = {
			NOT = {
				has_war_with = GRE
			}
		}
		activation = {
			has_country_flag = italian_conquest_flag
			has_war_with = GRE
			is_ai = yes
		}
		timeout_effect = {
		}
	}
	greece_survive = {
		days_mission_timeout = 365
		selectable_mission = no
		is_good = no
		allowed = {
			tag = GRE
		}
		available = {
			ITA = { casualties_k > 85 }
			NOT = {
				any_core_state = { is_controlled_by = ITA }
			}
		}
		activation = {
			has_war_with = ITA
			ITA = { has_country_flag = italian_conquest_flag }
			is_ai = no
		}
		complete_effect = { white_peace = ITA add_war_support = 0.025 }
		timeout_effect = {
				white_peace = ITA
		}
	}
}