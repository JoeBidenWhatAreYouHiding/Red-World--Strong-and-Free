#Tanks
swp_uralvagonzavod = {
    include = generic_infantry_tank_organization
    icon = GFX_idea_uralvagonzavod
    
    allowed = {	
        original_tag = SWP
    }

    equipment_type = { mio_cat_eq_all_light_tank mio_cat_eq_all_medium_tank mio_cat_eq_all_heavy_tank }
    research_categories = { mio_cat_tech_light_armor_and_modules mio_cat_tech_medium_armor_and_modules mio_cat_tech_heavy_armor_and_modules }

    initial_trait = {
        organization_modifier = {
            military_industrial_organization_research_bonus = 0.10
        }
    }
    
    available = {
        # When in a Foreign MIO, countries need to be at peace with original country
        IF = {
            limit = {
                FROM = { NOT = { original_tag = SWP } }
            }
            FROM = { NOT = { has_war_with = SWP } }
        }
    }
}

#Ships
swp_inace = {
    include = generic_task_force_ship_organization
    icon = GFX_idea_inace
    
    allowed = {	
        original_tag = SWP
    }

    initial_trait = {
        organization_modifier = {
            military_industrial_organization_research_bonus = 0.10
        }
        equipment_bonus = { fuel_consumption = -0.05 }
    }
    
    available = {
        # When in a Foreign MIO, countries need to be at peace with original country
        IF = {
            limit = {
                FROM = { NOT = { original_tag = SWP } }
            }
            FROM = { NOT = { has_war_with = SWP } }
        }
    }
}

#Planes
swp_avco = {
    include = generic_light_aircraft_organization
    icon = GFX_idea_avco
    name = Avco
    allowed = {	
        original_tag = SWP
    }

    initial_trait = {
        organization_modifier = {
            military_industrial_organization_research_bonus = 0.10
        }
        equipment_bonus = { air_attack = 0.05 air_agility = 0.05 }
    }
    
    available = {
        # When in a Foreign MIO, countries need to be at peace with original country
        IF = {
            limit = {
                FROM = { NOT = { original_tag = SWP } }
            }
            FROM = { NOT = { has_war_with = SWP } }
        }
    }
}

#Materiel
swp_windhoeker_maschinenfabrik = {
    include = generic_motorized_mechanized_organization
    icon = GFX_idea_wmf
    
    allowed = {	
        original_tag = SWP
    }

    initial_trait = {
        organization_modifier = {
            military_industrial_organization_research_bonus = 0.10
        }
        equipment_bonus = { defense = 0.05 soft_attack = 0.05 }
    }
    
    available = {
        # When in a Foreign MIO, countries need to be at peace with original country
        IF = {
            limit = {
                FROM = { NOT = { original_tag = SWP } }
            }
            FROM = { NOT = { has_war_with = SWP } }
        }
    }
}
swp_kalashnikov = {
    include = generic_infantry_equipment_organization
    icon = GFX_idea_kalashnikov
    
    allowed = {	
        original_tag = SWP
    }

    equipment_type = {
		infantry_equipment
		support_equipment
	}

    initial_trait = {
		equipment_bonus = {
			build_cost_ic = -0.05
			soft_attack = 0.05
		}
	}
    
    available = {
        # When in a Foreign MIO, countries need to be at peace with original country
        IF = {
            limit = {
                FROM = { NOT = { original_tag = SWP } }
            }
            FROM = { NOT = { has_war_with = SWP } }
        }
    }
}
swp_npo_splav = {
    include = generic_artillery_organization
    icon = GFX_idea_npo_splav
    
    allowed = {	
        original_tag = SWP
    }

    initial_trait = {
        organization_modifier = {
            military_industrial_organization_research_bonus = 0.10
        }
        equipment_bonus = { soft_attack = 0.1 }
    }
    
    available = {
        # When in a Foreign MIO, countries need to be at peace with original country
        IF = {
            limit = {
                FROM = { NOT = { original_tag = SWP } }
            }
            FROM = { NOT = { has_war_with = SWP } }
        }
    }
}