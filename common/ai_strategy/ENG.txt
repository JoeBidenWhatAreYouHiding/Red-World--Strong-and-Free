ENG_kill_ireland = {
	enable = {
		tag = ENG
		ENG = { 
		AND = {
		has_war_with = IRE 
		OR = {
		has_completed_focus = ENG_operation_green
		has_completed_focus = ENG_red_ireland
		has_completed_focus = ENG_unite_the_isles
				}
			}
		}
	}
	abort = {
	 OR = {
		ENG = {
        is_in_faction_with = IRE
		}
		IRE = {
		is_puppet_of = ENG
			}
		}
	}
	abort_when_not_enabled = yes
	ai_strategy = {
        type = invade
        id = IRE
        value = 5000
    }
}